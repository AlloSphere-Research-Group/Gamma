Put any files and directories in here. They will be built after you run cmake again.

Resources will be copied retaining the file structure here, so you can use relative paths in your code.

This file needs to be here because git doesn't allow empty directories!
